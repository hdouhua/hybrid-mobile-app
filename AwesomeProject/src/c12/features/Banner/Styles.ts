import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  iconStyle: {
    width: 50,
    height: 50,
    borderRadius: 10,
  },
  textStyle: {
    fontSize: 12,
    color: '#636363',
  },
});
