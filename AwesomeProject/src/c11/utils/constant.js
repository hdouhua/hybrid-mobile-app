/**
 *
 */
import {generateArrayData} from './dataUtil';

export const ITEM_HEIGHT = 100;

export const DATA = generateArrayData(0, 100);

export const ViewTypes = {
  FULL: 0,
  HALF_LEFT: 1,
  HALF_RIGHT: 2,
};

// mock data size
export const FETCH_BATCH_SIZE = 30;
export const FETCH_DATA_SIZE = 100;
